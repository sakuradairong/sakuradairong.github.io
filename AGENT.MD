# 提交与推送规则

本项目要求及时同步远端。每完成一次完整改动（包括代码、内容、样式、配置或文档），都必须在结束任务前完成提交和推送；这是用户的持续授权，无需再次询问是否提交或推送。用户在当前任务中明确要求暂缓时，以当前要求为准。

1. 检查本次差异，完成与改动相关的构建和校验；影响生成页面时，同步重新生成静态产物。
2. 仅暂存本次任务涉及的文件，以清晰说明改动的提交信息执行 `git commit`。
3. 立即推送到当前分支对应的远端分支；没有 upstream 时，先确认远端与分支，再设置 upstream 并推送。
4. 确认推送成功，并在回复中报告提交哈希和目标分支。提交或推送受阻时，说明原因及尚未完成的步骤；仅本地提交不算同步完成。

只读检查或没有实际差异时，无需创建空提交。推送使用普通方式，禁止未经用户明确授权的强制推送。
